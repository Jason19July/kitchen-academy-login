<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bread Masterclass - The Kitchen Academy</title>
    
    <!-- Memberstack Script -->
    <script
      data-memberstack-app="app_cmlt23jfd002u0stjhljd13s9"
      src="https://static.memberstack.com/scripts/v2/memberstack.js"
      type="text/javascript">
    </script>

    <style>
        body, html { margin: 0; padding: 0; height: 100%; overflow: hidden; font-family: sans-serif; }
        #protected-content { display: none; width: 100%; height: 100vh; border: none; }
        #access-denied { display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh; text-align: center; padding: 20px; }
        .btn { padding: 12px 24px; background: #000; color: #fff; border: none; border-radius: 5px; cursor: pointer; text-decoration: none; margin-top: 15px; }
    </style>
</head>
<body>

    <!-- This is what they see if they aren't logged in -->
    <div id="access-denied">
        <h1>Members Only Content</h1>
        <p>Please log in to access the Bread Masterclass.</p>
        <button data-ms-modal="login" class="btn">Log In Now</button>
    </div>

    <!-- This is the "Wrapper" for your Gamma Course -->
    <iframe id="protected-content" src="https://bread-masterclass-one-5bfi5ki.gamma.site/"></iframe>

    <script>
        // Use Memberstack to check the student's status
        window.$memberstackDom.getCurrentMember().then(({ data: member }) => {
            if (member) {
                // SUCCESS: They are logged in! Show the course, hide the warning.
                document.getElementById('protected-content').style.display = 'block';
                document.getElementById('access-denied').style.display = 'none';
            }
        });
    </script>

</body>
</html>
